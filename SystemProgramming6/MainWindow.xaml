<Window x:Class="SystemProgramming6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemProgramming6"
        mc:Ignorable="d"
        Title="М'ютекси та потоки" Height="450" Width="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Результат виконання потоків:" FontWeight="Bold" Margin="0,0,0,10"/>
        
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TextBox x:Name="OutputTextBox" 
                     IsReadOnly="True" 
                     TextWrapping="Wrap" 
                     VerticalScrollBarVisibility="Visible"
                     FontFamily="Consolas"
                     FontSize="14"/>
        </ScrollViewer>
        
        <Button Grid.Row="2" 
                x:Name="StartButton" 
                Content="Запустити потоки" 
                HorizontalAlignment="Center" 
                Padding="20,5" 
                Margin="0,10,0,0"
                Click="StartButton_Click"/>
    </Grid>
</Window>